~ start
if GameState.get_value(GameState.HAS_OILED_LEGS) != true:
	What is the code?
	if prompt("Enter locker code") == "1234":
		=> success
	else:
		You did not enter the correct code.
else
	This is where you found the oil.
=> END

~ success
You entered the code successfully, and you open the locker.
There is an oil can inside, so you use it to oil your legs. Now you can bend your knees!
do GameState.set_value(GameState.HAS_OILED_LEGS, true)
=> END