~ start
if RoomState.get_value("door_open")
	The door is open.
elif GameState.get_value(GameState.Flags.HAS_LEFT_ARM)
	You open the door with your left arm.
	do parent.rotate_y(-1.5)
	do RoomState.set_value("door_open", true)
else
	The door is closed, and you don't have any arms.
=> END